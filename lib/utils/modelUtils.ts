// TODO: use TypeScript wizardry to convert our custom model types to zod objects for validation
// This might be really hard, but it would be really cool and it would remove a lot of boiler-plate stuff
export const makeValidator = (type: any) => {
}
